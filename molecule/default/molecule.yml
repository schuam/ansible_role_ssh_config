---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu
    image: "schuam/docker_ubuntu2204_ansible"
    pre_build_image: true
  - name: arch
    image: "schuam/docker_archlinux_ansible"
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
